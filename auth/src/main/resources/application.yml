spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: cs994824@gmail.com
    password: mgbjguchbttsjddv
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true